server:
  port: 8020
  servlet:
    contextPath: /gateway

zuul:
  routes:
    user-api:
      path: /user-api/**
      serviceId: service-user
    task-api:
      path: /task-api/**
      serviceId: service-task