server:
  port: 8050
  servlet:
    contextPath: /gateway

zuul:
  routes:
    user-api:
      path: /user-api/**
      serviceId: service-user
    auth-api:
      path: /auth-api/**
      serviceId: service-auth